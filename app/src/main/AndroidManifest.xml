<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cobafp.com.cobafinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gambar_android"
        android:label="MI ONE"
        android:roundIcon="@mipmap/gambar_android"
        android:supportsRtl="true"
        android:theme="@style/Splash">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MatkulSatu"
            android:theme="@style/AppTheme" />
        <activity android:name=".MatkulDua" />
        <activity android:name=".MatkulTiga" />
        <activity android:name=".MatkulEmpat" />
        <activity android:name=".MatkulLima" />
        <activity android:name=".MatkulEnam" />
        <activity android:name=".Jadwal"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>